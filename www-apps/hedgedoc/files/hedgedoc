#!/sbin/openrc-run
command=/usr/bin/yarn
command_args="start --production"
pidfile=/var/run/hedgedoc.pid
name="Hedgedoc real-time collaborative markdown editor"
start_stop_daemon_args="--user hedgedoc --wait 5 --chdir /opt/hedgedoc -b --pidfile ${pidfile} -m"

depend() {
	need net
}
