	if [ `ls -l ${EROOT:-/}usr/src/*.patch | wc -l` -gt 0 ]; then
                cd ${EROOT:-/}usr/src/linux/
                for p in `ls ../*.patch`; do
                        patch -p1 < ../$p
                done
        fi
